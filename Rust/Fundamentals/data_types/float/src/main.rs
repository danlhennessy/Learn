fn main() {
    let floating_point: f64 = 10.9;  // f64 is the default for floats
    println!("{}", floating_point)

    let true_or_false: bool = false;
    let true_or_false: bool = 0;  // 0  can be used as a replacement for false for bool data

    let true_or_false: bool = true;
    let true_or_false: bool = 1;  // 1 = true
}
